diff --git a/node_modules/vaul/dist/index.js b/node_modules/vaul/dist/index.js
index 7f343be..79dee82 100644
--- a/node_modules/vaul/dist/index.js
+++ b/node_modules/vaul/dist/index.js
@@ -468,7 +468,7 @@ const TRANSITIONS = {
 const VELOCITY_THRESHOLD = 0.4;
 const CLOSE_THRESHOLD = 0.25;
 const SCROLL_LOCK_TIMEOUT = 100;
-const BORDER_RADIUS = 8;
+const BORDER_RADIUS = 40;
 const NESTED_DISPLACEMENT = 16;
 const WINDOW_TOP_OFFSET = 26;
 const DRAG_CLASS = 'vaul-dragging';
@@ -764,8 +764,17 @@ function useScaleBackground() {
                 }
             });
             return ()=>{
-                wrapperStylesCleanup();
+                // Animate wrapper back — property-level changes trigger CSS transition
+                if (wrapper) {
+                    set(wrapper, {
+                        borderRadius: '0px',
+                        overflow: 'visible',
+                        transform: 'none',
+                    }, true);
+                }
+                // Delay full cleanup until after transition completes
                 timeoutIdRef.current = window.setTimeout(()=>{
+                    wrapperStylesCleanup();
                     if (initialBackgroundColor) {
                         document.body.style.background = initialBackgroundColor;
                     } else {
@@ -1110,7 +1119,7 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
             if (wrapper && overlayRef.current && shouldScaleBackground) {
                 // Calculate percentageDragged as a fraction (0 to 1)
                 const scaleValue = Math.min(getScale() + percentageDragged * (1 - getScale()), 1);
-                const borderRadiusValue = 8 - percentageDragged * 8;
+                const borderRadiusValue = 40 - percentageDragged * 40;
                 const translateValue = Math.max(0, 14 - percentageDragged * 14);
                 set(wrapper, {
                     borderRadius: `${borderRadiusValue}px`,
diff --git a/node_modules/vaul/dist/index.mjs b/node_modules/vaul/dist/index.mjs
index 0dbbce5..4d5bd10 100644
--- a/node_modules/vaul/dist/index.mjs
+++ b/node_modules/vaul/dist/index.mjs
@@ -446,7 +446,7 @@ const TRANSITIONS = {
 const VELOCITY_THRESHOLD = 0.4;
 const CLOSE_THRESHOLD = 0.25;
 const SCROLL_LOCK_TIMEOUT = 100;
-const BORDER_RADIUS = 8;
+const BORDER_RADIUS = 40;
 const NESTED_DISPLACEMENT = 16;
 const WINDOW_TOP_OFFSET = 26;
 const DRAG_CLASS = 'vaul-dragging';
@@ -742,8 +742,17 @@ function useScaleBackground() {
                 }
             });
             return ()=>{
-                wrapperStylesCleanup();
+                // Animate wrapper back — property-level changes trigger CSS transition
+                if (wrapper) {
+                    set(wrapper, {
+                        borderRadius: '0px',
+                        overflow: 'visible',
+                        transform: 'none',
+                    }, true);
+                }
+                // Delay full cleanup until after transition completes
                 timeoutIdRef.current = window.setTimeout(()=>{
+                    wrapperStylesCleanup();
                     if (initialBackgroundColor) {
                         document.body.style.background = initialBackgroundColor;
                     } else {
@@ -1088,7 +1097,7 @@ function Root({ open: openProp, onOpenChange, children, onDrag: onDragProp, onRe
             if (wrapper && overlayRef.current && shouldScaleBackground) {
                 // Calculate percentageDragged as a fraction (0 to 1)
                 const scaleValue = Math.min(getScale() + percentageDragged * (1 - getScale()), 1);
-                const borderRadiusValue = 8 - percentageDragged * 8;
+                const borderRadiusValue = 40 - percentageDragged * 40;
                 const translateValue = Math.max(0, 14 - percentageDragged * 14);
                 set(wrapper, {
                     borderRadius: `${borderRadiusValue}px`,
